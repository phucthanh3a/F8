/*
var fullName = ' Le Phuc Thanh';
var age = 26;

alert(fullName);
alert(age);
*/

// Hàm built-in
// var fullName =' Le Phuc Thanh'
// console.log(fullName)


/**
 * BUILT-IN
 * 1. Alert " Hiển thị trên bảng thông báo"
 * vd: alert("bạn đã đủ 18 tuổi chưa")
 * 2. Console " Hiển thị nội dung trong phần console tính năng F12 của trang"
 * vd: console.log(Toi le Le Phuc Thanh)
 * 3. Confirm " Hiển thị bảng nội dung cần xác nhận"
 * vd: confirm('Nếu đã 18 tuổi vui lòng chọn xác nhận')
 * 4. Prompt " Hiển thị bảng cần điền nội dung"
 * vd: promt('điền số tuổi của bạn)
 * 5. Set timeout"Cho đoạn code chạy quảng thời gian nhất định "đơn vị mili giây"
 * vd: setTimeout(function(){
 *         alert('Thong bao')
 * },1000"ms")
 * 5. Set Interval"Cho đoạn code chạy quảng thời gian nhất định "đơn vị mili giây" nhưng chạy liên tục
 * vd: setInterval(function(){
 *         console.log('Day la thong bao' + Math.random())
 * },1000"ms")
 */



/**
 * TOÁN TỬ
 * 1. Toán tử số học - Arithmetic
 * vd:
 * var a = 1 + 2
 * console.log(a)
 * 2. Toán tử gán - Assignment
 * vd:
 * var fullName = 'Le Phuc Thanh'
 * console.log(fullName)
 * 3. Toán tử so sánh - Comparison
 * vd: 
 * var a = 1;
 * var b = 2;
 * if (a > b) {
 *      alert('Dung');
 * }
 * 4. Toán tử logic - Logical
 * vd:
 * var a = 1;
 * var b = 2;
 * if (a > 0 && b > 0) {
 *      alert('A&B lon hon 0');
 * }
 */




/**
 * TOÁN TƯT SỐ HỌC
 * 1.Cộng "+"
 * vd: 
 * var a = 1;
 * var b = 2;
 * var c = a + b:
 * console.log(c)
 * 2.Trừ "-"
 * vd: 
 * var a = 1;
 * var b = 2;
 * var c = a - b:
 * console.log(c)
 * 3.Nhân "*"
 * vd: 
 * var a = 1;
 * var b = 2;
 * var c = a * b:
 * console.log(c)
 * 4.Lũy thừa"**"
 * vd: 
 * var a = 1;
 * var b = 2;
 * var c = a ** b:
 * console.log(c)
 * 5.Chia "/"
 * vd: 
 * var a = 8;
 * var b = 2;
 * var c = a / b:
 * console.log(c)
 * 6.Chia lấy số dư "%"
 * vd: 
 * var a = 9;
 * var b = 2;
 * var c = a % b:
 * console.log(c)
 * 7.Tăng 1 giá trị số "++"
 * vd: 
 * var a = 1;
 * a++;
 * a++;
 * console.log(a)
 * 8. Giảm 1 giá trị số "--"
 * var a = 4;
 * a--;
 * a--;
 * console.log(a)
 */